# Preprocessing-experimental-images
This is a code used to identify effective pixels and cut them, and then expand them into squares, which is used for normalization and preprocessing of experimental pictures.
